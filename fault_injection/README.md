# fault_injection

Optional ROS2 node that injects faults (resource changes) during mission execution.

It is used by **test_campaign** for robustness evaluation. See the [documentation README](../documentation/README.md) for how it fits into the test campaign batches (`fault_injection` and `skill_failure`).
